# دليل إضافة البيانات التجريبية لنظام UberFix

## 📋 نظرة عامة

يحتوي ملف `database-seed-data.sql` على بيانات تجريبية شاملة لاختبار النظام، تشمل:
- ✅ 5 عقارات متنوعة (تجارية وسكنية)
- ✅ 7 موردين بتخصصات مختلفة
- ✅ 5 طلبات صيانة بحالات مختلفة
- ✅ 3 مواعيد
- ✅ 3 مصروفات

## 🚀 طريقة الاستخدام

### الطريقة 1: عبر Supabase SQL Editor (موصى بها)

1. **افتح Supabase Dashboard**
   ```
   https://supabase.com/dashboard/project/zrrffsjbfkphridqyais
   ```

2. **اذهب إلى SQL Editor**
   - من القائمة الجانبية، اختر "SQL Editor"
   - أو استخدم الرابط المباشر:
   ```
   https://supabase.com/dashboard/project/zrrffsjbfkphridqyais/sql/new
   ```

3. **انسخ محتوى الملف**
   - افتح ملف `database-seed-data.sql`
   - انسخ كل المحتويات

4. **الصق المحتوى وقم بالتنفيذ**
   - الصق المحتوى في SQL Editor
   - اضغط على زر "Run" أو `Ctrl+Enter`
   - انتظر حتى يكتمل التنفيذ

5. **تحقق من النتائج**
   - ستظهر رسائل تأكيد بعدد السجلات المضافة
   - تحقق من الجداول في Database Editor

### الطريقة 2: عبر أداة psql

إذا كنت تفضل استخدام سطر الأوامر:

```bash
psql "postgresql://postgres:[YOUR-PASSWORD]@db.zrrffsjbfkphridqyais.supabase.co:5432/postgres" -f database-seed-data.sql
```

## 📊 البيانات المضافة

### 1. العقارات (Properties)

| الاسم | الكود | النوع | المساحة | الموقع |
|------|------|------|---------|---------|
| برج السلام التجاري | PROP-xxx | تجاري | 2,500 م² | وسط البلد |
| مجمع الأندلس السكني | PROP-xxx | سكني | 5,000 م² | الهرم، الجيزة |
| مول النخيل التجاري | PROP-xxx | تجاري | 8,000 م² | الإسكندرية الصحراوي |
| فيلا الياسمين | PROP-xxx | سكني | 450 م² | التجمع الخامس |
| العمارة السكنية المدينة | PROP-xxx | سكني | 1,200 م² | مدينة نصر |

### 2. الموردين (Vendors)

| الاسم | الشركة | التخصصات | التقييم | المنطقة |
|------|--------|----------|---------|---------|
| أحمد محمود | شركة الصيانة المتكاملة | كهرباء، سباكة، نجارة | 4.8 ⭐ | مدينة نصر |
| محمد السيد | الكهربائي المحترف | كهرباء، إضاءة | 4.6 ⭐ | مصر الجديدة |
| خالد حسن | السباك الماهر | سباكة، صرف صحي | 4.9 ⭐ | المعادي |
| عمر فتحي | شركة النظافة الذهبية | تنظيف، تعقيم | 4.7 ⭐ | الدقي |
| ياسر علي | مكافحة الآفات المتقدمة | مكافحة حشرات | 4.5 ⭐ | الهرم |
| طارق عبدالله | فني التكييفات | تكييف، تبريد | 4.8 ⭐ | التجمع الخامس |
| حسام الدين | الدهان الماهر | دهانات، ديكور | 4.4 ⭐ | المهندسين |

### 3. طلبات الصيانة (Maintenance Requests)

| العنوان | الحالة | الأولوية | التكلفة |
|---------|--------|---------|----------|
| تسريب مياه في الحمام | Open | عاجل | 500 ج.م |
| عطل في التكييف المركزي | Assigned | متوسط | 800 ج.م |
| كسر في زجاج النافذة | In Progress | عادي | 300 ج.م |
| صيانة دورية للمصاعد | Pending Approval | عادي | 1,200 ج.م |
| تنظيف شامل للمبنى | Open | متوسط | 2,000 ج.م |

### 4. المواعيد (Appointments)

- معاينة موقع التسريب - بعد يومين الساعة 10:00 ص
- صيانة التكييف - غداً الساعة 2:00 م
- تركيب زجاج جديد - بعد 3 أيام الساعة 9:00 ص

### 5. المصروفات (Expenses)

- مواد (أنابيب وخراطيم) - 250 ج.م
- أجور (فريق التنظيف) - 800 ج.م
- معدات (صيانة التكييف) - 450 ج.م

## ⚠️ ملاحظات هامة

1. **الأكواد العشوائية**: يتم إنشاء أكواد عشوائية فريدة لكل عقار لتجنب التعارض
2. **البريد الإلكتروني**: يتم إضافة أحرف عشوائية لبريد الموردين لتجنب التكرار
3. **الارتباطات**: يتم ربط البيانات تلقائياً مع البيانات الموجودة (مستخدمين، شركات، فروع)
4. **RLS Policies**: جميع البيانات تحترم سياسات الأمان المطبقة

## 🔄 إعادة تشغيل البيانات

إذا أردت حذف البيانات التجريبية وإعادة إضافتها:

```sql
-- حذف البيانات التجريبية
DELETE FROM expenses WHERE description LIKE '%تجريب%' OR amount < 1000;
DELETE FROM appointments WHERE customer_name IN ('محمد أحمد', 'أحمد حسن', 'سارة محمود');
DELETE FROM maintenance_requests WHERE client_name IN ('محمد أحمد', 'أحمد حسن', 'سارة محمود', 'إدارة المبنى', 'مدير المشروع');
DELETE FROM vendors WHERE email LIKE '%@maintenance.com' OR email LIKE '%@electrician.com' OR email LIKE '%@plumber.com';
DELETE FROM properties WHERE code LIKE 'PROP-%';
```

ثم أعد تشغيل ملف `database-seed-data.sql`.

## 🧪 اختبار البيانات

بعد إضافة البيانات، يمكنك اختبارها من خلال:

### 1. التحقق من العدد
```sql
SELECT 
  (SELECT COUNT(*) FROM properties) as عقارات,
  (SELECT COUNT(*) FROM vendors) as موردين,
  (SELECT COUNT(*) FROM maintenance_requests) as طلبات,
  (SELECT COUNT(*) FROM appointments) as مواعيد,
  (SELECT COUNT(*) FROM expenses) as مصروفات;
```

### 2. عرض طلبات الصيانة مع التفاصيل
```sql
SELECT 
  mr.title as العنوان,
  mr.status as الحالة,
  mr.priority as الأولوية,
  p.name as العقار,
  v.name as المورد,
  mr.estimated_cost as التكلفة
FROM maintenance_requests mr
LEFT JOIN properties p ON mr.property_id = p.id
LEFT JOIN vendors v ON mr.assigned_vendor_id = v.id
ORDER BY mr.created_at DESC
LIMIT 10;
```

### 3. عرض الموردين مع تقييماتهم
```sql
SELECT 
  name as الاسم,
  company_name as الشركة,
  specialization as التخصصات,
  rating as التقييم,
  total_jobs as عدد_الأعمال,
  address as العنوان
FROM vendors
ORDER BY rating DESC;
```

## 🎯 الخطوات التالية

بعد إضافة البيانات التجريبية:

1. **تسجيل الدخول إلى النظام**
2. **تصفح العقارات** من صفحة Properties
3. **فحص طلبات الصيانة** من صفحة Maintenance Requests
4. **عرض الموردين** من صفحة Vendors
5. **التحقق من المواعيد** من صفحة Appointments
6. **مراجعة المصروفات** من صفحة Expenses

## 📞 الدعم

إذا واجهت أي مشاكل:
- تحقق من أن لديك الصلاحيات المناسبة (admin)
- تأكد من أن RLS policies مفعلة بشكل صحيح
- راجع سجل الأخطاء في Supabase Dashboard
