// Generated compatibility bridge for Capacitor Android builds.
// This file is intentionally committed because app/build.gradle references it during CI builds.

android {
  compileOptions {
    sourceCompatibility JavaVersion.VERSION_17
    targetCompatibility JavaVersion.VERSION_17
  }
}

dependencies {
  implementation project(':capacitor-android')
}
