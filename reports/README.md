# مجلد التقارير - Reports Directory

## 📋 الغرض
هذا المجلد مخصص لحفظ **جميع** تقارير المشروع. لا يُسمح بإنشاء تقارير في الجذر (root).

---

## 📂 البنية

```
reports/
├── README.md                    # هذا الملف
├── report.json                  # إعدادات مجلد التقارير
├── project-status-report.md     # تقرير حالة المشروع الحالي
├── logs/                        # سجلات التطبيق
├── tests/                       # نتائج الاختبارات
├── coverage/                    # تقارير التغطية
└── analysis/                    # تقارير التحليل المعماري
```

---

## 📊 أنواع التقارير

### 1. تقارير الحالة (Status Reports)
- **الملف:** `project-status-report.md`
- **التحديث:** يدوي أو عند طلب المراجعة
- **المحتوى:** حالة المشروع، المشاكل، الإجراءات المطلوبة

### 2. تقارير البنية المعمارية (Architecture Reports)
- **الملفات:** `architecture_report_*.txt`, `architecture_data_*.json`
- **التوليد:** عبر `scripts/run_architecture_analysis.sh`
- **المحتوى:** تحليل شامل لبنية الكود

### 3. تقارير الإصلاح (Repair Reports)
- **الملفات:** `repair_report_*.txt`
- **التوليد:** عبر `scripts/run_repair.sh`
- **المحتوى:** الأخطاء المكتشفة والإصلاحات المطبقة

### 4. تقارير الاختبارات (Test Reports)
- **المجلد:** `tests/`
- **التوليد:** عبر `pnpm test` أو `vitest`

### 5. تقارير التغطية (Coverage Reports)
- **المجلد:** `coverage/`
- **التوليد:** عبر `pnpm test:coverage`

---

## 🚫 القواعد الصارمة

1. ❌ **ممنوع** إنشاء تقارير في الجذر (root)
2. ✅ **جميع** التقارير يجب أن تكون في `reports/`
3. ✅ استخدام الأسماء الموحدة للتقارير
4. ✅ التنظيم حسب النوع في المجلدات الفرعية

---

## 🔧 كيفية إنشاء تقرير جديد

### تقرير الحالة
```bash
# يتم التحديث يدوياً أو عبر Lovable AI
```

### تقرير البنية المعمارية
```bash
bash scripts/run_architecture_analysis.sh
# النتائج تُحفظ في reports/ تلقائياً
```

### تقرير الإصلاح
```bash
bash scripts/run_repair.sh
# النتائج تُحفظ في reports/ تلقائياً
```

### تقارير الاختبارات
```bash
pnpm test                # تشغيل الاختبارات
pnpm test:coverage       # مع تغطية الكود
# النتائج في reports/tests/ و reports/coverage/
```

---

## 📝 ملاحظات مهمة

- **التاريخ والوقت:** جميع التقارير الآلية تتضمن timestamp في اسم الملف
- **النسخ الاحتياطي:** يُنصح بعمل backup دوري لهذا المجلد
- **Git:** المجلد مُضاف لـ Git لتتبع التغييرات في التقارير

---

**آخر تحديث:** 2025-01-21  
**الحالة:** 🟢 منظم وجاهز
